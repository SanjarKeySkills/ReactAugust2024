import React from "react";
import NotFound from "../../components/NotFound/NotFound";

const NotFoundPage = () => {
    return <NotFound />;
};

export default NotFoundPage;

// то есть есть компонент <NotFound /> а это страница, и она уже отражена в роуте в App.js
